import React from 'react';
import {default as  BaseResolver} from './base';

export default  class ButtonResolver extends BaseResolver  {
  constructor(dataSource, template, key) {
    super(dataSource, template, key);
  }
}
